# Tapir Backend Environment Configuration
# Copy this file to .env and customize as needed

# API Key (optional) - If set, all API requests must include:
# Authorization: Bearer <your-api-key>
# Leave empty to disable authentication
#TAPIR_API_KEY=your-secret-key-here

# CORS Origin (default: *)
# Set to specific domain in production, e.g., https://yourdomain.com
# Use * to allow all origins (not recommended for production)
TAPIR_CORS_ORIGIN=*

# Rate Limit (default: 60)
# Maximum requests per IP address per minute
TAPIR_RATE_LIMIT=60
